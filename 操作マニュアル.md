# 操作マニュアル

tauri-qwen-perfect の使い方。専門知識は不要です。
準備（インストール・Ollama）は同じフォルダの README.md を参照してください。

## いちばん基本の流れ

1. 解析したい文章をコピーする、または「📎 ファイル選択」でファイルを指定する。
2. 何をしてほしいかを入力欄に書く（下のテンプレートを押すだけでもOK）。
3. 青い「📋 クリップボードを解析」ボタンを押す（`Cmd + Enter` でも実行）。

回答は上から順に表示されます。ファイルを選んでいるときは、クリップボードではなく
そのファイルの中身を解析します。

## 入力の渡し方（3通り）

- **コピー**：他アプリで文章をコピー（Cmd + C）してから実行。
- **ファイル選択**：「📎 ファイル選択」ボタンから選ぶ。
- **ドラッグ&ドロップ**：ウィンドウにファイルを放り込む。

対応ファイル：pdf / docx / xlsx / txt / csv / md / 画像（png・jpg・jpeg・webp）。

## テンプレート（よく使う指示のボタン）

- **📝 ビジネス校正**：文章を整える。
- **🛡️ マスク・匿名化**：氏名や固有名詞を伏せる。
- **🔍 原因特定・分析**：内容を分析して要点を出す。
- **📄 箇条書き要約**：重要な点を数点にまとめる。

ボタンを押すと入力欄に指示が入るので、そのまま実行するか、手直しして実行します。

## 画像の文字読み取り（OCR）

画像（png・jpg・jpeg・webp）を選ぶと、中の文字を書き起こしてから指示を実行します。
スキャン書類やスクリーンショットをそのまま要約・校正できます。
初回は画像用モデルの取得（`ollama pull qwen2.5vl`）が必要です。

書き起こしに徹する作りなので、そのまま「📄 箇条書き要約」や「🛡️ マスク・匿名化」を
当てられます。

## 結果の保存（エクスポート）

回答の下のボタンで保存できます：md / txt / csv / docx / pdf / xlsx。
表形式の回答は xlsx でセルに分かれて保存されます。
初回だけ保存先フォルダを選ぶと、次回から自動でそこへ保存されます。

## 記憶機能（社内資料の検索）

手元の資料フォルダを読み込ませておき、質問に関係する箇所だけを自動で探して
回答の根拠に使えます。すべて端末内で完結します。

1. 「🧠 フォルダを記憶」で、資料の入ったフォルダを選ぶ（txt・md などのテキスト系）。
2. 「記憶を参照」にチェックを入れて質問すると、関連箇所が回答に反映されます。
3. 「🗑」で記憶をすべて消せます。

初回は検索用モデルの取得（`ollama pull bge-m3`）が必要です。

## 自律エージェント

「🤖 エージェント」にチェックを入れると、AIが読み取り専用のツール
（フォルダ一覧・ファイル読取・記憶検索）を自分で使いながら調べて回答します。
書き込み・削除などの操作は安全のため持たせていません。

## モデルの切り替え

「モデル」のドロップダウンで、Ollama に入っているモデルを選べます。

## プライバシー

読み込んだ文書・画像・対話履歴は外部に送信されません。AIの処理も検索も、
すべて手元のPC内（localhost）で完結します。
